{
  "name": "ciel-os",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.2",
  "engines": {
    "bun": ">=1.3.2 <2"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "bun run typecheck && bun test",
    "events:validate": "bun run bin/ciel.ts events validate",
    "wake": "bun run bin/ciel.ts wake"
  },
  "bin": {
    "ciel": "./bin/ciel.ts"
  },
  "dependencies": {
    "yaml": "2.9.0"
  },
  "devDependencies": {
    "@types/bun": "1.3.2",
    "typescript": "7.0.2"
  }
}
