schema_version: ciel.event.v0.1
id: evt_20260906T191945_ledger_lane_authorized
type: decision
recorded_at: 2026-09-06T19:19:45+07:00
recorded_by:
  human: owner
  agent: claude

workstream:
  id: pilot-ledger-count-command-001
  lane: single
  objective: >-
    Authorize plan revision 0.3 slice 1, the count command for the ledger CLI, so this
    workstream can serve as one of the two concurrent cold lanes in the
    many-sessions proof.
  scope:
    - Implement the one change this plan describes in pilot-task-ledger, with a test.
    - Merge it locally onto the child's main and record a closeout in HQ.
  out_of_scope:
    - Any change to the task JSON contract the two pilots share, or to anything the sibling project reads.
    - Any new dependency, and any pull request for the child, which is local_only.
    - Any change to CIEL source or contract.

outcome:
  status: decided
  parallelism: approved
  authority_source:
    - The owner authorized ciel-parallel-lanes-001 revision 0.3 slice 2, which requires two concurrent workstreams sharing no child project.
    - The owner directed that both lanes be driven by sessions given no context from the planning conversation, so the disposable-session claim is tested rather than assumed.
    - The owner directed that the child work stay trivial, so that a failure is attributable to the arrangement rather than to the feature.
  completed:
    - Authorizes plan revision 0.3 slice 1 on the single lane.
    - Approves parallel execution alongside pilot-report-status-flag-001.
  not_completed:
    - Nothing is changed by this decision. It records authority only.

evidence:
  repository:
    head: 4f6d1784c371cb1ea7e26bbde9ee975c15b33bcb
    branch: hq/20260906
  plan: workstreams/pilot-ledger-count-command-001/PLAN.md
  plan_revision: "0.3"
  slice: "1"
  parallel_with:
    - pilot-report-status-flag-001
  overlap_justification: >-
    This lane touches pilot-task-ledger only and the concurrent lane touches its own
    child only. They meet on ciel-os alone, which no longer counts as an
    overlap since slice 1 separated HQ from workstream status.
  cold_start_condition: >-
    The driving session receives no context from the planning conversation. If
    it cannot proceed from Wake and the repository, that is a recorded gap in
    what the repository carries, not a failure of the session.

unresolved:
  - The two pilots share a task JSON contract, so the lanes are independent only while this one leaves that contract untouched.
  - Whether this lane's HQ commits collide with the other lane's on the shared standing branch is unproven; staging only this workstream's own paths is the rule that prevents it.

next_action:
  action: >-
    Run Wake, read this plan, implement the single change with a test, merge it
    locally onto the child's main, and record a closeout on the standing HQ
    branch staging only this workstream's own paths.
  acceptance_check:
    - Wake must report this workstream as authorized for plan revision 0.3 slice 1 before any child file changes.
    - The child's existing tests must still pass and its unchanged output must stay byte-identical.
