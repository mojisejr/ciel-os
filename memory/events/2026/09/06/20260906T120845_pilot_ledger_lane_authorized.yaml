schema_version: ciel.event.v0.1
id: evt_20260906T120845_pilot_ledger_lane_authorized
type: decision
recorded_at: 2026-09-06T12:08:45+07:00
recorded_by:
  human: owner
  agent: claude

workstream:
  id: pilot-ledger-count-command-001
  lane: single
  objective: >-
    Authorize slice 1, a count command for the ledger CLI, so that this
    workstream can serve as one of the two concurrent lanes in the
    parallel-lanes proof.
  scope:
    - Add a count command to the ledger CLI that prints the number of valid tasks.
    - Cover it with a test and merge it locally onto the child's main.
  out_of_scope:
    - Any change to Task, TaskPriority, the exported JSON shape, or anything pilot-task-report reads.
    - Any new dependency, and any pull request for the child, which is local_only.
    - Any change to CIEL code or contract; that belongs to ciel-parallel-lanes-001 slice 2.

outcome:
  status: decided
  parallelism: approved
  authority_source:
    - The owner authorized the parallel-lanes proof, which requires two concurrent workstreams that share no child project.
    - The owner directed that the child work stay trivial so that a failure is attributable to the lane mechanics rather than to the feature.
  completed:
    - Authorizes plan revision 0.1 slice 1 on the single lane.
    - Approves parallel execution alongside pilot-report-status-flag-001 and ciel-parallel-lanes-001.
  not_completed:
    - Nothing is changed by this decision. It records authority only.

evidence:
  repository:
    head: e91316a99aecfdaf205f03acc1cb1a6c610112d1
    branch: docs/parallel-lanes-dogfood
  plan: workstreams/pilot-ledger-count-command-001/PLAN.md
  plan_revision: "0.1"
  slice: "1"
  parallel_with:
    - pilot-report-status-flag-001
  overlap_justification: >-
    This lane touches pilot-task-ledger only. The concurrent lane touches
    pilot-task-report only. They overlap on ciel-os alone, which the owner's
    rule does not count.

unresolved:
  - The two pilots share a task JSON contract, so independence holds only while this lane leaves that contract untouched.
  - This lane is driven by the session that prepared the plan and therefore carries context a cold lane would not have; the cold-start claim is tested by the other lane.

next_action:
  action: >-
    After the plans land on main, create this lane's worktree from the primary
    worktree, bootstrap its local bindings with absolute child paths, implement
    the count command, and open its HQ draft pull request.
  acceptance_check:
    - Wake must report this workstream as authorized for plan revision 0.1 slice 1 before the lane worktree is created.
    - The child's existing tests and the unchanged export output must both still pass.
