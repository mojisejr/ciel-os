schema_version: ciel.event.v0.1
id: evt_20260905T141950_slice4_claude_bridge
type: closeout
recorded_at: 2026-09-05T14:19:50+07:00
recorded_by:
  human: owner
  agent: claude

workstream:
  id: ciel-multi-client-support-001
  lane: single
  objective: >-
    Add the Claude bridge as one explicitly delimited CIEL-managed section that
    holds no rule of its own, and amend the AGENTS.md rules that forbade it.
  scope:
    - Amend the rule forbidding CLAUDE.md and the line naming Codex as the only client.
    - State the bridge discipline once, in the shared foundation.
    - Add CLAUDE.md containing pointers and the one rule that must reach a client before it opens AGENTS.md.
  out_of_scope:
    - The cross-client proof, which slice 5 carries and which no decision authorizes yet.
    - Any bridge generator, verification command, or Wake awareness of the bridge file.
    - Any IDE instruction file, nested instruction file, or third client.

outcome:
  status: ready-for-owner-merge
  owner_authorization: >-
    Decision event evt_20260905T141744_slice4_authorized authorizes plan revision
    0.2 slice 4. It was committed before any slice 4 change so the authorization
    could be observed rather than assumed.
  completed:
    - Replaced the Codex-only client line with one naming both clients and stating that CLAUDE.md holds nothing but pointers.
    - Narrowed the prohibition to IDE-specific instructions, nested instruction files, and a third bridge, each still gated on a reviewed proof gap.
    - Added the bridge discipline to Instruction-file hygiene, so the rule lives in the shared foundation rather than only in this workstream's plan.
    - Added CLAUDE.md with one delimited CIEL-managed section and an explicit note that content outside it is preserved.
  not_completed:
    - No proof that a fresh Claude session actually follows the bridge. That is slice 5 and it may fail.
    - CIEL does not read, verify, or render the bridge. Nothing in Wake knows the file exists.

evidence:
  repository:
    head: b72e6a55f4234808b451e2a2ddaeb8c5dc56e337
    branch: feat/slice4-claude-bridge
  plan: workstreams/ciel-multi-client-support-001/PLAN.md
  plan_revision: "0.2"
  slice: "4"
  authorization_proof:
    method: >-
      The decision event was committed first, then a temporary clone was placed
      on a branch named main with origin/main pointed at that commit, so the
      lifecycle could be read as it will be after merge. The clone was removed.
    observed: >-
      Wake reported ciel-multi-client-support-001 as authorized with the detail
      "Owner decision authorizes plan revision 0.2 slice 4" and named the
      decision event as its source.
    significance: >-
      This is the first slice-keyed authorization to take effect, and therefore
      the working proof that slice 2 repaired a mechanism that could not
      authorize anything under the current plan policy.
  bridge:
    path: CLAUDE.md
    size: 20 lines, of which 12 carry content inside the managed section
    delimiters: ciel:managed:start and ciel:managed:end, with a stated promise that content outside is preserved
    contents_traced:
      - Pointer to AGENTS.md as the operating contract. A pointer, not a rule.
      - Run bun run wake before a substantive answer or file change, and without needing a slash command. Restates AGENTS.md line 22.
      - The Wake evidence boundary, including that reaching for an excluded source before Wake is how this contract gets broken. Restates AGENTS.md lines 33, 34 and 36.
    why_restated_not_referenced: >-
      Reference is preferred everywhere else, and the bridge references AGENTS.md
      for everything but this. The evidence boundary is the exception because it
      governs the moment before AGENTS.md is opened. A client that has not yet
      read the contract cannot be told by the contract not to reach for a global
      skill first, which is precisely the recorded failure. The plan's invariant
      4 requires this restatement.
  removal_test:
    method: CLAUDE.md was moved out of the repository, then bun run wake and bun run check were run, then it was restored.
    result: Wake succeeded and all 28 tests passed with the bridge absent.
    proves: The bridge holds no canonical state. CIEL remains fully operable through AGENTS.md and README.md alone.
  size:
    agents_md: 90 lines to 91
    added: One line stating the bridge discipline in Instruction-file hygiene.
    replaced: The client line and the prohibition, each one line for one line.
    justification: >-
      The added line traces to the recorded friction where a universal rule sat
      only in a completed workstream's plan. Without it the bridge discipline
      would repeat that pattern by living only in this plan.
  checks:
    - bun run typecheck
    - bun run test
    - bun run bin/ciel.ts events validate
    - bun run bin/ciel.ts wake
  result: Typecheck passed, 28 tests passed, all events validate with no warnings, and Wake reports no validation errors.

unresolved:
  - Whether a fresh Claude session follows this bridge is unproven and is the whole of slice 5. A bridge sync is a rendering, never proof of obedience.
  - CIEL cannot audit the bridge. Wake reports whether AGENTS.md is present but knows nothing about CLAUDE.md, so a bridge that drifts or is deleted is invisible. Whether CIEL should verify it is a slice 5 finding, not an assumption.
  - The managed-section delimiters are a convention this file declares. Nothing enforces them, and no command rewrites or checks the section.
  - The supported-client set under Genesis Contract v0.2 section 14 remains unratified. This slice adds a bridge; it does not settle the roster.
  - Genesis Contract v0.2 section 11 asks for two bridges as its V0 proof. Both now exist, but the proof itself is slice 5.

next_action:
  action: >-
    After this merges, record a decision authorizing slice 5, then run the proof:
    a fresh Claude session with no steering and a fresh Codex session must each
    perform their own Wake and report the same state, next action, and unknowns
    for one nominated workstream. Record any divergence as a finding rather than
    smoothing it over.
  acceptance_check:
    - This closeout must be committed and pushed to its draft pull-request head and proven an ancestor of that head before the pull request is advanced for owner review.
    - Slice 5 must not begin before a decision event authorizes it and Wake reports the workstream as authorized for that slice.
    - The proof must run without either session reading this conversation or any other client's chat history.
